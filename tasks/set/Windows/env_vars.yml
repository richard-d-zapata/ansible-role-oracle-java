---
- name: Set java_installers_path (Windows)
  set_fact:
    java_installers_path: "{{ lookup('env', 'JAVA_WINDOWS_INSTALLERS_PATH' ) }}"
  when: lookup('env', 'JAVA_WINDOWS_INSTALLERS_PATH' ) is defined and lookup('env', 'JAVA_WINDOWS_INSTALLERS_PATH' ) != ''
